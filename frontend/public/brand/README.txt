Place your preferred PNG logo here as either:

- Sabi1.png  (from provided assets)
- Sabi3.png  (from provided assets)

The app will automatically pick the file configured in src/branding.ts and fall back to the existing SVG logo if not present.

Recommended sizes:
- Wordmark: 800×160 (will be downscaled)
- Icon/Monogram: 256×256 or 512×512

Background: Use transparent background to blend with the dark theme.