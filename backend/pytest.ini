[pytest]
pythonpath = src
addopts = -ra --cov=src --cov-fail-under=52 --ignore=test_model_balanced.py --ignore=test_scraper.py
asyncio_mode = auto
# Ignore standalone test scripts that have import issues when run with full suite
testpaths = tests
norecursedirs = scripts
