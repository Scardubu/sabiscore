[pytest]
pythonpath = src
addopts = -ra --ignore=test_model_balanced.py --ignore=test_scraper.py
asyncio_mode = auto
# Ignore standalone test scripts that have import issues when run with full suite
testpaths = tests
norecursedirs = scripts
