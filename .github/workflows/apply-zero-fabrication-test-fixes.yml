name: Apply zero-fabrication test fixes

on:
  push:
    branches: [codex/final-production-certification]

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/final-production-certification
          fetch-depth: 0
      - name: Apply reviewed test contract patch
        run: |
          set -euo pipefail
          test -f scripts/apply-zero-fabrication-test-fixes.py
          python scripts/apply-zero-fabrication-test-fixes.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "test: enforce fail-closed regression expectations"
          git push origin HEAD:codex/final-production-certification
